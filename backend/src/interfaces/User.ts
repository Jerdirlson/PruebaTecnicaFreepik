export interface User {
    idUser?: number;
    numId? : string,
    nameUser : string,
    emailUser : string,
    pwdUser : string,
}

export interface UserUpdate{
    nameUser : string,
    emailUser : string,
    pwdUser : string,
}